#include <stdio.h>

/**
 * mod - This function returns the remainder of a given calculation
 * @a: First integer
 * @b: secound integer
 * Return: success
 */

int mod(int a, int b)
{
	return (a % b);
}
